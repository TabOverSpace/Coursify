<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanyogghosh.coursify">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Introduction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".First"
            android:label="@string/title_activity_first"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".Fourth"
            android:label="@string/title_activity_fourth"
            android:parentActivityName=".First"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.sanyogghosh.coursify.First" />
        </activity>
        <activity android:name=".Recommended" />
        <activity android:name=".Intro" />
        <activity
            android:name=".First2"
            android:label="@string/title_activity_first2"
            android:theme="@style/AppTheme" />
        <activity android:name=".Course_page"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>